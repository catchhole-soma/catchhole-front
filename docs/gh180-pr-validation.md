# GH180 프론트 PR 검증 — 2026-09-13

최신 main `7cd479347c4c1f3448c876fe43f6667cb278f95d`의 이메일 가입, 접근성 개선,
세계관 공유 판단 수정 및 영상 Hero를 보존하며 회차별 자동 반영 흐름을 통합했다.
생성 SDK는 통합 Java API의 실제 `/v3/api-docs`에서 재생성했다.

- 단일 자동 반영 기본값, 직접 검토 선택, 다회차 10회 선행 조건 제거와 250,000자 제한을 검증했다.
- 자동 저장 완료 시각까지 결과 이동과 후보 변경을 잠그며, 이전 완료 회차의 검토는 유지한다.
- main의 기존 범위 병합 바로가기에도 연결된 후보 전체의 자동 저장 대기 잠금을 적용했다.
- 영상·추출 장면·CTA 시점은 그대로 두고 일반 서비스 설명을 자동 반영 기본값과 맞췄다.
- 업로드 카드와 미확정 안내 사이를 20px로 띄웠다. 현재 사이트와 PR 코드 모두 1280/320px에서 간격 20px·가로 넘침 없음을 확인했다.

## 검증

- `npm run build`: TypeScript와 production build 통과. 기존 bundle 크기 안내는 남아 있다.
- `npm run lint`: 오류 0, 기존 Fast Refresh 경고 2.
- `npm run check:unused`: 통과. 기존 Knip entry 중복 안내 1.
- `CATCHHOLE_E2E_PORT=3100 npm run test:e2e -- --workers=4`: 305 통과, 사전 계정이 필요한 live 2개 제외. 이전 완료 문구를 기대한 테스트 1개가 실패했다.
- 해당 기대 문구와 자동 저장 완료 시각 fixture를 현재 계약에 맞춘 뒤 `... npm run test:e2e -- e2e/upload-policy.spec.ts --workers=4`: 18개 전체 통과. 합쳐서 최종 306개 시나리오가 확인됐다.
- `npm run doctor`: 참고 검사 실행. 169개 진단(오류 분류 1 포함)을 모두 해결했다는 뜻은 아니다. 오류 분류가 가리킨 WorldSettingDatabase의 기존 선택 콜백은 이번 PR에서 변경되지 않았으며 광범위한 UI 정리는 별도 범위다. lint/typecheck/build 통과와 Doctor 무진단을 혼동하지 않는다.

[데스크톱 여백](screens/gh180-upload-spacing-1280.png) · [320px 여백](screens/gh180-upload-spacing-320.png)

검증은 합성 응답과 별도 브라우저 컨텍스트를 사용했다. 사용자의 로그인·작품·후보는 변경하지 않았다.
Java/API와 AI Worker의 새 계약을 먼저 반영한 뒤 이 프론트를 배포한다. 이 작업은 PR 생성이며 운영 배포를 수행하지 않는다.
# 2026-09-13 리뷰 후속

- 취소·무효화된 분석은 과거 토큰 중단 건수보다 취소 상태를 우선한다. 결과 검토 대신 진행 화면에서 취소 사유를 확인한다.
- 저장 완료 기록이 불완전하고 재개 가능한 회차가 없으면 무반응 재개 버튼 대신 비활성 `설정 저장 상태 확인 필요`를 표시한다. 이 상태를 정상적인 저장 지연이나 분석 성공으로 단정하지 않는다.
- 순차 분석의 토큰 부족은 원래 회차 작업을 재개하는 기존 경로를 유지한다. 서버는 순차 분석에 `tokenInterruptedAfterExtraction=true`를 반환하지 않으므로 기존 확정 설정 모드의 세계관 전용 재개 화면으로 전환하지 않는다.
- 모의 API 브라우저 검증: `analysis-list.spec.ts`, `ordered-analysis.spec.ts`. 취소 안내는 1280px와 320px에서 확인하고, 정상 재개 및 기존 결과 진입 경로도 유지한다. 실제 분석 요청은 생성하지 않는다.

## 두 번째 리뷰 반영

- 회차별 자동 반영 완료 표시도 공통 완료 판정을 사용한다. 성공·저장 기록 확정뿐 아니라 자동 반영 완료 시각이 있어야 완료로 표시하여 상단 안내와 회차 상태를 맞췄다.
- 같은 작업을 재개할 때 사용량 소진 알림을 재개 시도별로 구분한다. 같은 실패를 반복 조회하면 알림을 중복 표시하지 않고, 재개가 수락된 뒤 다시 소진되면 새 알림을 표시한다. 이전 비동기 알림의 완료 처리가 새 시도의 알림을 지우지 않도록 별도 키를 쓴다.
- `ordered-analysis.spec.ts`, `token-interruption.spec.ts` **29개 통과**. 동일 작업의 재개·재소진, 중복 조회 알림 억제, 저장 완료 표시 및 기존 중단 복구를 모의 API로 검증했다. production build 통과, lint 오류 0·기존 Fast Refresh 경고 2개다. 실제 사용자 분석이나 사용량은 변경하지 않았다.

## 세 번째 리뷰 반영

- 캐릭터 직접 검토 안내에 전용 의미 클래스와 Theme V2 배경·테두리 토큰을 사용한다. 기존의 넓은 CSS 덮어쓰기 밖으로 옮긴 복제 안내도 4.5:1 이상의 대비를 유지하는지 1280px·320px 브라우저 시나리오에서 검증한다.
- 사용량 부족으로 중단된 세계관 후보는 `manualReviewAvailable`이나 이전 수동 결정이 남아 있어도 직접 수정·그룹 확정을 열지 않는다. 버튼뿐 아니라 수정/확정 요청 생성에서도 현재 후보 상태를 확인한다. 일반 실패·검토 보류의 직접 확인과 제외, 기존 순차 재개는 유지한다.
- `automatic-review.spec.ts`, `world-setting-review-reasons.spec.ts`, `ordered-analysis.spec.ts` 모의 API **44개 통과**. 수정/확정 요청 생성 방어를 추가한 최종 코드에서 앞의 두 파일 **33개를 재검증해 통과**했다. production build 통과, lint 오류 0·기존 경고 2개다. 실제 사용자 후보를 변경하거나 실제 분석을 실행하지 않았다.

## 네 번째 리뷰 반영

- 재시도로 새 작업 ID를 받으면 이전 작업은 추적 이력에 보존하되 반복 조회를 멈춘다. 현재 작업은 완료 뒤 무효화 확인을 위해 계속 조회한다.
- 무효화된 작업 일부만 먼저 조회되어도 나머지 현재 작업의 응답을 받기 전에는 새 분석을 시작하지 않는다. 진행 중인 회차가 확인되면 확인창의 제출과 요청 처리도 같은 조건으로 잠근다.
- 원문 교체 후 단일 회차 재분석은 `reviewMode=MANUAL`을 명시한다. 확정 설정을 자동 변경하지 않는 기존 안내와 실제 요청을 맞추며, 새 업로드의 자동 반영 기본값은 유지한다.
- 캐릭터 단건·일괄 연결 변경 성공 시 요청에 포함된 후보의 직접 검토 완료 표시만 해제한다. 연결 실패는 기존 검토와 입력을 보존한다. 새 대상으로 비교 중이거나 비교 실패로 돌아온 경우 이전 검토로 확정할 수 없고, 새로 직접 확인해 저장하면 다시 확정할 수 있다.

검증은 별도 로컬 포트 `43177`의 모의 API 브라우저에서 수행했다. 실제 사용자 작품·원고·분석·사용량과 3000 사이트는 변경하지 않았다.

- `npm run typecheck`, `npm run build`, `npm run check:unused` 통과.
- `npm run lint`: 오류 0, 기존 Fast Refresh 경고 2. React Doctor 참고 검사 실행; 기존 범위의 진단을 해결했다고 주장하지 않는다.
- `ordered-analysis.spec.ts`, `automatic-review.spec.ts`, `smoke.spec.ts` 최초 실행은 55개 통과. 새 테스트 3개의 잘못된 기대 순서·화면 문구·갱신 트리거를 현재 정책에 맞췄다. 순차 분석 파일 전체를 재실행하여 **14개 전부 통과**했고, 최초 실행에서 나머지 두 파일 44개가 통과했다. 검증에는 새 ID 재개 후 조회 횟수, 지연된 형제 회차 응답, 열린 확인창에서 진행 상태 변경, 단건·일괄 연결 성공/실패, 원문 교체 재분석 요청 본문의 수동 검토 지정이 포함된다.
- `git diff --check` 통과. 전체 브라우저 회귀는 PR의 최종 커밋 CI에서 다시 검사한다.
